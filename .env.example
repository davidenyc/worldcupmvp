# =========================================
# SUPABASE — Auth + Postgres
# =========================================
# From Supabase dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="sb_publishable_..."

# SECRET — never commit, never expose to client. Project Settings → API → service_role.
SUPABASE_SERVICE_ROLE_KEY=""

# Postgres connection strings — Project Settings → Database → Connection string
# DATABASE_URL uses pgbouncer (port 6543) for app runtime / Prisma queries
# DIRECT_URL is the direct connection (port 5432) used only by `prisma migrate`
DATABASE_URL="postgresql://postgres.YOUR-REF:PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.YOUR-REF:PASSWORD@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# =========================================
# App
# =========================================
NEXT_PUBLIC_APP_URL="https://gamedaymap.com"
ELITE_ACCESS_SECRET="" # REQUIRED IN PRODUCTION — generate with: openssl rand -hex 32

# Map provider: "leaflet" (free) or "mapbox"
NEXT_PUBLIC_MAP_PROVIDER="leaflet"
NEXT_PUBLIC_MAPBOX_TOKEN=""

# Google Places API (for venue data)
GOOGLE_PLACES_API_KEY=""
GOOGLE_PLACES_TEXT_SEARCH_URL="https://places.googleapis.com/v1/places:searchText"

# Data source: "mock" | "csv" | "google"
DATA_PROVIDER="mock"
CSV_IMPORT_PATH="./data/imports/venues.csv"

# =========================================
# Email (transactional) — Resend
# =========================================
RESEND_API_KEY=""

# =========================================
# Notifications
# =========================================
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:hello@gamedaymap.com"
CRON_SECRET=""
