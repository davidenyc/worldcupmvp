# Real API Integration TODO

- Replace the demo NYC 2D map panel with a production map adapter while preserving clustering and marker mini-card behavior.
- Merge multiple providers into one normalized aggregation layer with provenance-aware conflict resolution.
- Implement official geocoding and neighborhood assignment with caching and backoff.
- Persist submission, reservation request, and report-update flows with Prisma server actions.
- Add auth and roles with Clerk or NextAuth.
- Implement open-now logic from official provider hours data.
- Support CSV upload parsing, preview mapping, and bulk import review UI in `/admin`.
- Add duplicate confidence scoring, source-quality scoring, and merge history.
- Store photos through approved sources only.
- Add real match schedules and venue-level watch-party associations.
- Add shareable links, favorites, and matchday mode.
- Expand beyond NYC with city-scoped routing.
